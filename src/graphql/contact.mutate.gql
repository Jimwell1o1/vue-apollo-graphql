mutation contactMutate (
  $name: String!
  $email: String!
  $contact: String!
  $address: String!
  $message: String!
  )
{
  createContact (
    name: $name
    email: $email
    contact: $contact
    address: $address
    message: $message
  ) {
    id
    name

  }
}